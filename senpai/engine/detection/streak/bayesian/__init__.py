"""Bayesian rate-track registration engine, selected by `streak.registration_engine`.

A self-contained alternative to the modules in the parent package: it shares every
utility that is identical to theirs and carries its own copy only where the maths
differs. Imported only when the dispatch selects it.
"""
