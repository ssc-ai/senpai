"""FITS header keywords that carry a frame's date and time.

Several spellings are in use across sensors, so readers try each in order rather than assuming
one.
"""

DATE_TIME_HEADERS = ["DATE-OBS"]
DATE_HEADERS = ["DATE_OBS"]
TIME_HEADERS = ["TIME-OBS"]
